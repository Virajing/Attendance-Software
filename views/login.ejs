<!DOCTYPE html>
<html lang="en">
<head>
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <div class="container mx-auto mt-20">
    <h1 class="text-3xl font-bold text-center">Login</h1>
    <form action="/submit-login" method="POST" class="max-w-md mx-auto mt-10 bg-white p-5 rounded shadow">
      <input 
        type="text" 
        name="username" 
        placeholder="Username" 
        required 
        class="w-full p-2 border mb-3"
        value="<%= typeof username !== 'undefined' ? username : '' %>">
      <input 
        type="password" 
        name="password" 
        placeholder="Password" 
        required 
        class="w-full p-2 border mb-3">
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded w-full">Login</button>

      <!-- Display error message if present -->
      <% if (error) { %>
        <p class="text-red-500 text-center mt-3"><%= error %></p>
      <% } %>
    </form>
  </div>
</body>
</html>
